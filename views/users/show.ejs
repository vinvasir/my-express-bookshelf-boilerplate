<% include ../_header %>

<p><%= user.name %></p>

<p><%= user.username %></p>

<p><%= user.email %></p>

 		<ul>
			<% user.posts.forEach(post => { %>

				<li>
					<strong><%= post.title %>:</strong>

					<%= post.body %>
				</li>
			<% }) %>
		</ul>

<% include ../_footer %>